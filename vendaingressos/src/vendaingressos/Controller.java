package vendaingressos;

import java.util.ArrayList;
import java.util.List;
import java.util.Date;

/*public class Controller {
    private List<Usuario> usuarios = new ArrayList<>();
    private List<Evento> eventos = new ArrayList<>();

    //Construtor
    public Controller(Usuario usuario, Ingresso ingresso, Evento evento) {
        this.usuario = usuario;
        this.ingresso = ingresso;
        this.evento = evento;
    }


    //Metodos teste
    public void cadastrarUsuario(String login,String senha, String cpf, String email) {
        Usuario novoUsuario = new Usuario(login, senha, cpf, email);
        usuarios.add(novoUsuario);
        return;
    }

    public void loginUsuario(String login, String senha) {
        for (Usuario usuario : usuarios) {
            if (usuario.getLogin().equals(login) && usuario.getSenha().equals(senha)) {
                usuario = usuario;
                return;
            } else {
                System.out.println("Login ou senha incorretos");
                return;
            }
        }
    }

    public void cadastrarEvento(String nome, String descricao, Date data) {
        Evento novoEvento = new Evento(nome, descricao, data);
        eventos.add(novoEvento);
    }


    public void listarEventosDisponiveis() {
        for (Evento evento : eventos) {
            System.out.println(evento);
        }
    }

    public void comprarIngresso(Evento evento) {

    }


    public void cancelarCompraIngresso() {

    }
}*/
